# SmartSepsis-Oph — production runtime dependencies.
#
# NOTE: These pins are APPROXIMATE minimum versions inferred from `import`
# statements across root-level *.py modules. They are NOT a locked
# reproducible environment. For reproducibility, generate a lock file with:
#
#     pip install pip-tools
#     pip-compile requirements.txt -o requirements.lock
#
# or use `uv pip compile requirements.txt`.

# --- Core scientific stack ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# --- Bioinformatics ---
biopython>=1.81

# --- HTTP / data fetch ---
requests>=2.31

# --- Deep learning / protein language models ---
torch>=2.1
transformers>=4.36
fair-esm>=2.0.0          # `import esm` — Meta AI ESM / ESMFold

# --- HuggingFace datasets (build_hf_dataset*.py) ---
datasets>=2.16

# --- Serialization / model artifacts ---
joblib>=1.3

# --- Optional but referenced indirectly via README / future entry points ---
# matplotlib>=3.7
# seaborn>=0.13
# tqdm>=4.66
# pyyaml>=6.0
# click>=8.1
